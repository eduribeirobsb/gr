<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Visualizar Ações do Risco</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">
  <h2>Visualizar Ações</h2>
  <p><strong>Contrato:</strong> <%= contrato.numero_contrato %> — <%= contrato.objeto %></p>
  <p><strong>Risco:</strong> <%= risco.risco %> — <strong>NRI:</strong> <%= risco.nri %></p>

  <% if (acoes.length === 0) { %>
    <div class="alert alert-info">Nenhuma ação cadastrada para este risco.</div>
  <% } else { %>
    <table class="table table-bordered mt-3">
      <thead class="table-light">
        <tr>
          <th>Tipo</th>
          <th>Descrição</th>
          <th>Responsável</th>
          <th>Situação</th>
          <th>Data de Atualização</th>
        </tr>
      </thead>
      <tbody>
        <% acoes.forEach(a => { %>
          <tr>
            <td><%= a.tipo %></td>
            <td><%= a.descricao %></td>
            <td><%= a.responsavel %></td>
            <td><%= a.situacao %></td>
            <td><%= a.data_atualizacao %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>

  <a href="/riscos/gerenciar/<%= contrato.id %>" class="btn btn-outline-secondary mt-3">
    ← Voltar para Riscos do Contrato
  </a>
</body>
</html>
