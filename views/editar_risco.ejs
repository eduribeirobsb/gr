<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Editar Risco</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-4">
  <h2>Editar Risco</h2>
  <form method="POST" action="/riscos/editar/<%= risco.id %>">
    <div class="mb-3">
      <label>Risco</label>
      <input type="text" name="risco" value="<%= risco.risco %>" class="form-control" required>
    </div>
    <div class="mb-3">
      <label>Causa</label>
      <input type="text" name="causa" value="<%= risco.causa %>" class="form-control" required>
    </div>
    <div class="mb-3">
      <label>Consequência</label>
      <input type="text" name="consequencia" value="<%= risco.consequencia %>" class="form-control" required>
    </div>
    <div class="mb-3">
      <label>Probabilidade (1 a 5)</label>
      <input type="number" name="probabilidade" min="1" max="5" value="<%= risco.probabilidade %>" class="form-control" required>
    </div>
    <div class="mb-3">
      <label>Impacto (1 a 5)</label>
      <input type="number" name="impacto" min="1" max="5" value="<%= risco.impacto %>" class="form-control" required>
    </div>
    <button class="btn btn-primary">Salvar Alterações</button>
    <a href="/riscos/gerenciar/<%= risco.contrato_id %>" class="btn btn-secondary">Cancelar</a>
  </form>
</body>
</html>
